# 🚀 PR Preview Platform

A self-hosted platform that automatically creates ephemeral preview environments for every Pull Request — with full CI pipeline, quality gates, and a real-time dashboard.